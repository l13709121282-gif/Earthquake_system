# 早期版本（已归档）

基于 STM32 + ESP8266 的第一代地震预警系统。

- **STM32F103**：负责 MPU6050 传感器数据采集（I2C 寄存器级驱动）
- **ESP8266**：负责 WiFi 连接和 MQTT 数据透传（TLS 加密）
- **云端 Python**：传感器数据趋势分析 + SiliconFlow AI 预测

此版本已被 ESP32 双核方案取代，保留作为项目演进参考。
不再更新维护。
